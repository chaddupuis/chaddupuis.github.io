ARG NODE_IMAGE=""
FROM node:${NODE_IMAGE} as chadyyhmsgfront

WORKDIR /app

# Tailwind and Astro installs may pull Git-sourced deps, so ensure git is available
RUN apt-get update \
	&& apt-get upgrade -y \
	&& apt-get install -y git \
	&& rm -rf /var/lib/apt/lists/*
